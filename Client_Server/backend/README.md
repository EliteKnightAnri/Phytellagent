This folder contains backend-related artifacts merged from Client_Server_Backend for integration.

Files included:
- `pyproject.toml` (reference)
- `README.md` (original project README)
- `mcp_settings.json` is placed at repository root (`Client_Server/mcp_settings.json`).

Notes:
- Secrets such as database passwords should be moved to environment variables or a `.env` file and not kept in this repository.
- Remove or adjust absolute paths if any remain.
